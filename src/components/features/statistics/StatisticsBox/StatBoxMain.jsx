/* eslint-disable react/prop-types */

function StatBoxMain({ children }) {
  return (
    <div className="w-full h-[90%] flex items-center justify-center relative">
      {children}
    </div>
  );
}

export default StatBoxMain;
